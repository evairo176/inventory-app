import CustomerPage from "@/components/pages/customers/customer-page";

import React from "react";

type Props = {};

const page = (props: Props) => {
  return <CustomerPage />;
};

export default page;
