import { Dashboard } from "@/components/dashboard/dashboard";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Dashboard />
    </div>
  );
};

export default page;
