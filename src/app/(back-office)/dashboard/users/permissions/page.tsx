import React from "react";
import PermissionPage from "@/components/pages/permission/permission-page";

type Props = {};

const page = (props: Props) => {
  return <PermissionPage />;
};

export default page;
